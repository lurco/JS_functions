import {cycleArray} from "./0211_cycleArray.js";

export function majorScale(key = 'c'){
    const whiteKeys = ['c', 'd', 'e', 'f', 'g', 'a', 'b'];
    const circleOfFifths = whiteKeys.map((note, i) => cycleArray())

    return scale;
}
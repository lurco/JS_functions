export function upTheCircleOfFifths(startingNote){
    const whiteKeys = 'fcgdaeb'.split('');
    const blackKeys = ['bb', 'f#', 'eb', 'ab', 'db'];

    const twelveTones = [blackKeys[0]].concat(whiteKeys).concat(blackKeys.slice(1));
    class Note{
        constructor(name, num,) {
        }
    }


}